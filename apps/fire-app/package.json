{
  "name": "@poc-nx-firebase/fire-app",
  "description": "Firebase Functions package, auto generated by @simondotm/nx-firebase",
  "scripts": {
    "lint": "cd ../../ && nx lint fire-app",
    "build": "cd ../../ && nx build fire-app --with-deps",
    "serve": "npm run build && firebase emulators:start --only functions --config ../../firebase.fire-app.json",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions --config ../../firebase.fire-app.json",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "14"
  },
  "main": "src/index.js",
  "dependencies": {},
  "devDependencies": {},
  "private": true
}
